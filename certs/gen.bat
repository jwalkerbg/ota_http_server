echo [v3_req]>certs\server_ext.cnf && echo basicConstraints = CA:FALSE>>certs\server_ext.cnf && echo keyUsage = digitalSignature, keyEncipherment>>certs\server_ext.cnf && echo extendedKeyUsage = serverAuth>>certs\server_ext.cnf && echo subjectAltName = DNS:localhost>>certs\server_ext.cnf && openssl req -x509 -nodes -days 3650 -newkey rsa:4096 -keyout certs\ca_key.pem -out certs\ca_cert.pem -subj "/CN=MyCA" -config "E:\apps\Apache24\conf\openssl.cnf" && openssl req -nodes -newkey rsa:4096 -keyout certs\server_key.pem -out certs\server_csr.pem -subj "/CN=localhost" -config "E:\apps\Apache24\conf\openssl.cnf" && openssl x509 -req -in certs\server_csr.pem -CA certs\ca_cert.pem -CAkey certs\ca_key.pem -CAcreateserial -out certs\server_cert.pem -days 3650 -sha256 -extfile certs\server_ext.cnf -extensions v3_req
